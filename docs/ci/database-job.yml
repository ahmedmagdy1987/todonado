# MOVED — this file is not the source of truth.
#
# The database and PostgREST jobs live in the canonical workflow:
#
#     .github/workflows/ci.yml
#       job `database`  -> "database (postgres 17, disposable)"
#       job `supabase`  -> "supabase postgrest permissions (local stack)"
#
# They were parked here for two iterations because the fine-grained PAT lacked
# the Workflows repository permission and GitHub refuses a push that edits
# .github/workflows without it. That permission was granted, so the jobs moved
# to where they actually run. Nothing is maintained in two places.
#
# Run the same checks locally:
#
#     npm install --no-save embedded-postgres   # real PG binaries, ~100 MB
#     npm run db:start
#     DATABASE_URL=postgres://postgres@localhost:55432/postgres npm run db:apply
#     DATABASE_URL=postgres://postgres@localhost:55432/postgres npm run test:db
#
#     supabase start                            # needs Docker
#     eval "$(supabase status -o env | sed 's/^/export /')"
#     SUPABASE_URL="$API_URL" SUPABASE_ANON_KEY="$ANON_KEY" \
#       SUPABASE_SERVICE_ROLE_KEY="$SERVICE_ROLE_KEY" npm run test:postgrest
